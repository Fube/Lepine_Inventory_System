import ShowStock from "../pages/stock"

/**
 * @param {{
 * deletable: boolean,
 * handleDelete: (uuid: string) => void,
 * handleSubmit: ({ values, setSubmitting: (isSubmitting: boolean)=>void }) => void }
 * & import(Stock) } )
 */

export default function StockForm() {}